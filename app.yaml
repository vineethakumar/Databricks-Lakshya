   command: ["uvicorn", "backend.app:app", "--host", "0.0.0.0", "--port", "$DATABRICKS_APP_PORT"]
   env:
     - name: "DATABRICKS_CATALOG"
       value: "telecom_qoe_catalog"
     - name: "DATABRICKS_SCHEMA"
       value: "telecom_qoe"
